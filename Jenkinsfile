pipeline {
    agent any

    stages {
        stage('clone') {
            steps {
              
            }
        }
        stage('init') {
            steps {
                sh 'terraform init'
            }
        }
    }
}
